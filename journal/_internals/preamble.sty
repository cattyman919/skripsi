% If IEEEtran.cls has not been installed into the LaTeX system files,
% manually specify the path to it like:
% \documentclass[journal]{../sty/IEEEtran}

% *** MISC UTILITY PACKAGES ***
\usepackage{ifpdf} % For conditional compilation (pdf/dvi)

% *** CITATION PACKAGES ***
% \usepackage{cite} % REMOVED: Incompatible with biblatex used in main file.
                   % biblatex with ieee style is used instead.

% *** GRAPHICS RELATED PACKAGES ***
\ifCLASSINFOpdf
  \usepackage[pdftex]{graphicx}
  % declare the path(s) where your graphic files are
  % \graphicspath{{../pdf/}{../jpeg/}} % Example path definition
  \graphicspath{{../assets/pics/}} % Correct path for your structure
  % and their extensions so you won't have to specify these with
  % every instance of \includegraphics
  \DeclareGraphicsExtensions{.pdf,.jpeg,.png,.jpg} % Added common extensions
\else
  % or other class option (dvipsone, dvipdf, if not using dvips). graphicx
  % will default to the driver specified in the system graphics.cfg if no
  % driver is specified.
  \usepackage[dvips]{graphicx}
  % declare the path(s) where your graphic files are
  \graphicspath{{../eps/}}
  % and their extensions so you won't have to specify these with
  % every instance of \includegraphics
  \DeclareGraphicsExtensions{.eps}
\fi
% graphicx is loaded above

% *** MATH PACKAGES ***
\usepackage{amsmath} % Essential for math environments
\interdisplaylinepenalty=2500 % Allow page breaks in multiline equations

% *** SPECIALIZED LIST PACKAGES ***
% \usepackage{algorithmic} % REMOVED: Not used in current content.

% *** ALIGNMENT PACKAGES ***
\usepackage{array} % Enhances tabular and array environments
\usepackage{tabularx} % For tables with adjustable columns (used in results)
\usepackage{multirow} % For cells spanning multiple rows/columns (used in results)
\usepackage{booktabs} % For professional quality tables (toprule, midrule, bottomrule) (used in results)


% *** SUBFIGURE PACKAGES ***
% \usepackage[caption=false,font=footnotesize]{subfig} % REMOVED: Not used in current content.
% Note: IEEEtran recommends caption=false if subfig is used.

% *** FLOAT PACKAGES ***
\usepackage{stfloats} % Provides \fnbelowfloat and allows bottom floats [!b]
\fnbelowfloat % Place footnotes below bottom floats
\usepackage{float}

% *** PDF, URL AND HYPERLINK PACKAGES ***
\usepackage{url} % Provides \url command, often used with hyperref
\usepackage{hyperref} % Creates hyperlinks in the document
\hypersetup{ % Configuration for hyperref (moved from journal.tex)
    colorlinks=true,
    linkcolor=black,    % Warna untuk tautan internal (misal: ke section, gambar)
    filecolor=black, % Warna untuk tautan ke file lokal
    urlcolor=black,     % Warna untuk URL eksternal
    citecolor=black,    % cite color
    pdftitle={Effectiveness of Code Virtualization using VxLang}, % Example title
    pdfpagemode=FullScreen, % Example setting
    pdfauthor={Seno Pamungkas Rahman}, % Add author metadata
    bookmarksopen=true,
    bookmarksnumbered=true,
    pdfborder={0 0 0} % Menghilangkan border jika colorlinks=false (opsional, tapi baik jika colorlinks=true)
    }

% *** PLOTTING PACKAGES ***
\usepackage{pgfplots} % For creating plots within LaTeX (used for QuickSort graph)
\pgfplotsset{compat=1.18} % Set compatibility for pgfplots (moved from journal.tex)
\usetikzlibrary{arrows.meta} % Load tikz library if needed by plots

\usepackage{xcolor}
\usepackage{minted}
% correct bad hyphenation here
\hyphenation{op-tical net-works semi-conduc-tor re-verse en-gi-neer-ing vir-tu-al-i-za-tion}

% ========================= CUSTOM COMMANDS ===============================
% (Keep your custom commands here if they are general purpose)
% Perintah untuk membuat perintah/variabel baru.
\newcommand{\var}[2]{\newcommand{#1}{#2}}

% Perintah untuk membuat perintah/variabel baru. Teks yang ditulis dalam
% perintah ini akan diformat ulang menggunakan huruf kapital.
\newcommand{\Var}[2]{\newcommand{#1}{\uppercase{#2}}}

% Alias for easier code formatting if needed (requires \usepackage{listings} or \usepackage{minted})
% \usepackage{minted} % Example: Add minted if you want code highlighting
% \setminted{style=default, fontsize=\small, breaklines=true} % Example minted config
\newcommand{\code}[1]{\texttt{#1}} % Simple monospace font for code

% Alias for italic font (as used in skripsi)
\newcommand{\f}[1]{\textit{#1}}
% Alias for bold font (as used in skripsi)
\newcommand{\bo}[1]{\textbf{#1}}
\newcommand{\bi}[1]{\textbf{\textit{#1}}}
